# Card translations

Each language lives in its own file: `en.js`, `he.js`, etc.

## Adding a new language

1. Copy `en.js` to `xx.js` (e.g. `fr.js` for French)
2. Translate all values
3. In `index.js`, add the import and include the new language in the TRANSLATIONS object
